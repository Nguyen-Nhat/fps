// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/predicate"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/processingfilerow"
)

// ProcessingFileRowQuery is the builder for querying ProcessingFileRow entities.
type ProcessingFileRowQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ProcessingFileRow
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProcessingFileRowQuery builder.
func (pfrq *ProcessingFileRowQuery) Where(ps ...predicate.ProcessingFileRow) *ProcessingFileRowQuery {
	pfrq.predicates = append(pfrq.predicates, ps...)
	return pfrq
}

// Limit adds a limit step to the query.
func (pfrq *ProcessingFileRowQuery) Limit(limit int) *ProcessingFileRowQuery {
	pfrq.limit = &limit
	return pfrq
}

// Offset adds an offset step to the query.
func (pfrq *ProcessingFileRowQuery) Offset(offset int) *ProcessingFileRowQuery {
	pfrq.offset = &offset
	return pfrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pfrq *ProcessingFileRowQuery) Unique(unique bool) *ProcessingFileRowQuery {
	pfrq.unique = &unique
	return pfrq
}

// Order adds an order step to the query.
func (pfrq *ProcessingFileRowQuery) Order(o ...OrderFunc) *ProcessingFileRowQuery {
	pfrq.order = append(pfrq.order, o...)
	return pfrq
}

// First returns the first ProcessingFileRow entity from the query.
// Returns a *NotFoundError when no ProcessingFileRow was found.
func (pfrq *ProcessingFileRowQuery) First(ctx context.Context) (*ProcessingFileRow, error) {
	nodes, err := pfrq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{processingfilerow.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) FirstX(ctx context.Context) *ProcessingFileRow {
	node, err := pfrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProcessingFileRow ID from the query.
// Returns a *NotFoundError when no ProcessingFileRow ID was found.
func (pfrq *ProcessingFileRowQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfrq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{processingfilerow.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) FirstIDX(ctx context.Context) int {
	id, err := pfrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProcessingFileRow entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProcessingFileRow entity is found.
// Returns a *NotFoundError when no ProcessingFileRow entities are found.
func (pfrq *ProcessingFileRowQuery) Only(ctx context.Context) (*ProcessingFileRow, error) {
	nodes, err := pfrq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{processingfilerow.Label}
	default:
		return nil, &NotSingularError{processingfilerow.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) OnlyX(ctx context.Context) *ProcessingFileRow {
	node, err := pfrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProcessingFileRow ID in the query.
// Returns a *NotSingularError when more than one ProcessingFileRow ID is found.
// Returns a *NotFoundError when no entities are found.
func (pfrq *ProcessingFileRowQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfrq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{processingfilerow.Label}
	default:
		err = &NotSingularError{processingfilerow.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) OnlyIDX(ctx context.Context) int {
	id, err := pfrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProcessingFileRows.
func (pfrq *ProcessingFileRowQuery) All(ctx context.Context) ([]*ProcessingFileRow, error) {
	if err := pfrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pfrq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) AllX(ctx context.Context) []*ProcessingFileRow {
	nodes, err := pfrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProcessingFileRow IDs.
func (pfrq *ProcessingFileRowQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pfrq.Select(processingfilerow.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) IDsX(ctx context.Context) []int {
	ids, err := pfrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pfrq *ProcessingFileRowQuery) Count(ctx context.Context) (int, error) {
	if err := pfrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pfrq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) CountX(ctx context.Context) int {
	count, err := pfrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pfrq *ProcessingFileRowQuery) Exist(ctx context.Context) (bool, error) {
	if err := pfrq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pfrq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pfrq *ProcessingFileRowQuery) ExistX(ctx context.Context) bool {
	exist, err := pfrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProcessingFileRowQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pfrq *ProcessingFileRowQuery) Clone() *ProcessingFileRowQuery {
	if pfrq == nil {
		return nil
	}
	return &ProcessingFileRowQuery{
		config:     pfrq.config,
		limit:      pfrq.limit,
		offset:     pfrq.offset,
		order:      append([]OrderFunc{}, pfrq.order...),
		predicates: append([]predicate.ProcessingFileRow{}, pfrq.predicates...),
		// clone intermediate query.
		sql:    pfrq.sql.Clone(),
		path:   pfrq.path,
		unique: pfrq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FileID int64 `json:"file_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProcessingFileRow.Query().
//		GroupBy(processingfilerow.FieldFileID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pfrq *ProcessingFileRowQuery) GroupBy(field string, fields ...string) *ProcessingFileRowGroupBy {
	grbuild := &ProcessingFileRowGroupBy{config: pfrq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pfrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pfrq.sqlQuery(ctx), nil
	}
	grbuild.label = processingfilerow.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FileID int64 `json:"file_id,omitempty"`
//	}
//
//	client.ProcessingFileRow.Query().
//		Select(processingfilerow.FieldFileID).
//		Scan(ctx, &v)
func (pfrq *ProcessingFileRowQuery) Select(fields ...string) *ProcessingFileRowSelect {
	pfrq.fields = append(pfrq.fields, fields...)
	selbuild := &ProcessingFileRowSelect{ProcessingFileRowQuery: pfrq}
	selbuild.label = processingfilerow.Label
	selbuild.flds, selbuild.scan = &pfrq.fields, selbuild.Scan
	return selbuild
}

func (pfrq *ProcessingFileRowQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pfrq.fields {
		if !processingfilerow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pfrq.path != nil {
		prev, err := pfrq.path(ctx)
		if err != nil {
			return err
		}
		pfrq.sql = prev
	}
	return nil
}

func (pfrq *ProcessingFileRowQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProcessingFileRow, error) {
	var (
		nodes = []*ProcessingFileRow{}
		_spec = pfrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ProcessingFileRow).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ProcessingFileRow{config: pfrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pfrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pfrq *ProcessingFileRowQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pfrq.querySpec()
	_spec.Node.Columns = pfrq.fields
	if len(pfrq.fields) > 0 {
		_spec.Unique = pfrq.unique != nil && *pfrq.unique
	}
	return sqlgraph.CountNodes(ctx, pfrq.driver, _spec)
}

func (pfrq *ProcessingFileRowQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pfrq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (pfrq *ProcessingFileRowQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   processingfilerow.Table,
			Columns: processingfilerow.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: processingfilerow.FieldID,
			},
		},
		From:   pfrq.sql,
		Unique: true,
	}
	if unique := pfrq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pfrq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, processingfilerow.FieldID)
		for i := range fields {
			if fields[i] != processingfilerow.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pfrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pfrq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pfrq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pfrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pfrq *ProcessingFileRowQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pfrq.driver.Dialect())
	t1 := builder.Table(processingfilerow.Table)
	columns := pfrq.fields
	if len(columns) == 0 {
		columns = processingfilerow.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pfrq.sql != nil {
		selector = pfrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pfrq.unique != nil && *pfrq.unique {
		selector.Distinct()
	}
	for _, p := range pfrq.predicates {
		p(selector)
	}
	for _, p := range pfrq.order {
		p(selector)
	}
	if offset := pfrq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pfrq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProcessingFileRowGroupBy is the group-by builder for ProcessingFileRow entities.
type ProcessingFileRowGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pfrgb *ProcessingFileRowGroupBy) Aggregate(fns ...AggregateFunc) *ProcessingFileRowGroupBy {
	pfrgb.fns = append(pfrgb.fns, fns...)
	return pfrgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pfrgb *ProcessingFileRowGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pfrgb.path(ctx)
	if err != nil {
		return err
	}
	pfrgb.sql = query
	return pfrgb.sqlScan(ctx, v)
}

func (pfrgb *ProcessingFileRowGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pfrgb.fields {
		if !processingfilerow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pfrgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfrgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pfrgb *ProcessingFileRowGroupBy) sqlQuery() *sql.Selector {
	selector := pfrgb.sql.Select()
	aggregation := make([]string, 0, len(pfrgb.fns))
	for _, fn := range pfrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pfrgb.fields)+len(pfrgb.fns))
		for _, f := range pfrgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pfrgb.fields...)...)
}

// ProcessingFileRowSelect is the builder for selecting fields of ProcessingFileRow entities.
type ProcessingFileRowSelect struct {
	*ProcessingFileRowQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pfrs *ProcessingFileRowSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pfrs.prepareQuery(ctx); err != nil {
		return err
	}
	pfrs.sql = pfrs.ProcessingFileRowQuery.sqlQuery(ctx)
	return pfrs.sqlScan(ctx, v)
}

func (pfrs *ProcessingFileRowSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pfrs.sql.Query()
	if err := pfrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
