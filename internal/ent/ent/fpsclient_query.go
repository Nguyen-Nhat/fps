// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/fpsclient"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/predicate"
)

// FpsClientQuery is the builder for querying FpsClient entities.
type FpsClientQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FpsClient
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FpsClientQuery builder.
func (fcq *FpsClientQuery) Where(ps ...predicate.FpsClient) *FpsClientQuery {
	fcq.predicates = append(fcq.predicates, ps...)
	return fcq
}

// Limit adds a limit step to the query.
func (fcq *FpsClientQuery) Limit(limit int) *FpsClientQuery {
	fcq.limit = &limit
	return fcq
}

// Offset adds an offset step to the query.
func (fcq *FpsClientQuery) Offset(offset int) *FpsClientQuery {
	fcq.offset = &offset
	return fcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fcq *FpsClientQuery) Unique(unique bool) *FpsClientQuery {
	fcq.unique = &unique
	return fcq
}

// Order adds an order step to the query.
func (fcq *FpsClientQuery) Order(o ...OrderFunc) *FpsClientQuery {
	fcq.order = append(fcq.order, o...)
	return fcq
}

// First returns the first FpsClient entity from the query.
// Returns a *NotFoundError when no FpsClient was found.
func (fcq *FpsClientQuery) First(ctx context.Context) (*FpsClient, error) {
	nodes, err := fcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fpsclient.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fcq *FpsClientQuery) FirstX(ctx context.Context) *FpsClient {
	node, err := fcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FpsClient ID from the query.
// Returns a *NotFoundError when no FpsClient ID was found.
func (fcq *FpsClientQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fpsclient.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fcq *FpsClientQuery) FirstIDX(ctx context.Context) int {
	id, err := fcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FpsClient entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FpsClient entity is found.
// Returns a *NotFoundError when no FpsClient entities are found.
func (fcq *FpsClientQuery) Only(ctx context.Context) (*FpsClient, error) {
	nodes, err := fcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fpsclient.Label}
	default:
		return nil, &NotSingularError{fpsclient.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fcq *FpsClientQuery) OnlyX(ctx context.Context) *FpsClient {
	node, err := fcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FpsClient ID in the query.
// Returns a *NotSingularError when more than one FpsClient ID is found.
// Returns a *NotFoundError when no entities are found.
func (fcq *FpsClientQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fpsclient.Label}
	default:
		err = &NotSingularError{fpsclient.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fcq *FpsClientQuery) OnlyIDX(ctx context.Context) int {
	id, err := fcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FpsClients.
func (fcq *FpsClientQuery) All(ctx context.Context) ([]*FpsClient, error) {
	if err := fcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return fcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (fcq *FpsClientQuery) AllX(ctx context.Context) []*FpsClient {
	nodes, err := fcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FpsClient IDs.
func (fcq *FpsClientQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := fcq.Select(fpsclient.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fcq *FpsClientQuery) IDsX(ctx context.Context) []int {
	ids, err := fcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fcq *FpsClientQuery) Count(ctx context.Context) (int, error) {
	if err := fcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return fcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (fcq *FpsClientQuery) CountX(ctx context.Context) int {
	count, err := fcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fcq *FpsClientQuery) Exist(ctx context.Context) (bool, error) {
	if err := fcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return fcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (fcq *FpsClientQuery) ExistX(ctx context.Context) bool {
	exist, err := fcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FpsClientQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fcq *FpsClientQuery) Clone() *FpsClientQuery {
	if fcq == nil {
		return nil
	}
	return &FpsClientQuery{
		config:     fcq.config,
		limit:      fcq.limit,
		offset:     fcq.offset,
		order:      append([]OrderFunc{}, fcq.order...),
		predicates: append([]predicate.FpsClient{}, fcq.predicates...),
		// clone intermediate query.
		sql:    fcq.sql.Clone(),
		path:   fcq.path,
		unique: fcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID int32 `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FpsClient.Query().
//		GroupBy(fpsclient.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fcq *FpsClientQuery) GroupBy(field string, fields ...string) *FpsClientGroupBy {
	grbuild := &FpsClientGroupBy{config: fcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := fcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return fcq.sqlQuery(ctx), nil
	}
	grbuild.label = fpsclient.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID int32 `json:"client_id,omitempty"`
//	}
//
//	client.FpsClient.Query().
//		Select(fpsclient.FieldClientID).
//		Scan(ctx, &v)
func (fcq *FpsClientQuery) Select(fields ...string) *FpsClientSelect {
	fcq.fields = append(fcq.fields, fields...)
	selbuild := &FpsClientSelect{FpsClientQuery: fcq}
	selbuild.label = fpsclient.Label
	selbuild.flds, selbuild.scan = &fcq.fields, selbuild.Scan
	return selbuild
}

func (fcq *FpsClientQuery) prepareQuery(ctx context.Context) error {
	for _, f := range fcq.fields {
		if !fpsclient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fcq.path != nil {
		prev, err := fcq.path(ctx)
		if err != nil {
			return err
		}
		fcq.sql = prev
	}
	return nil
}

func (fcq *FpsClientQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FpsClient, error) {
	var (
		nodes = []*FpsClient{}
		_spec = fcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FpsClient).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FpsClient{config: fcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fcq *FpsClientQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fcq.querySpec()
	_spec.Node.Columns = fcq.fields
	if len(fcq.fields) > 0 {
		_spec.Unique = fcq.unique != nil && *fcq.unique
	}
	return sqlgraph.CountNodes(ctx, fcq.driver, _spec)
}

func (fcq *FpsClientQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := fcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (fcq *FpsClientQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   fpsclient.Table,
			Columns: fpsclient.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: fpsclient.FieldID,
			},
		},
		From:   fcq.sql,
		Unique: true,
	}
	if unique := fcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := fcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fpsclient.FieldID)
		for i := range fields {
			if fields[i] != fpsclient.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fcq *FpsClientQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fcq.driver.Dialect())
	t1 := builder.Table(fpsclient.Table)
	columns := fcq.fields
	if len(columns) == 0 {
		columns = fpsclient.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fcq.sql != nil {
		selector = fcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fcq.unique != nil && *fcq.unique {
		selector.Distinct()
	}
	for _, p := range fcq.predicates {
		p(selector)
	}
	for _, p := range fcq.order {
		p(selector)
	}
	if offset := fcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FpsClientGroupBy is the group-by builder for FpsClient entities.
type FpsClientGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fcgb *FpsClientGroupBy) Aggregate(fns ...AggregateFunc) *FpsClientGroupBy {
	fcgb.fns = append(fcgb.fns, fns...)
	return fcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (fcgb *FpsClientGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := fcgb.path(ctx)
	if err != nil {
		return err
	}
	fcgb.sql = query
	return fcgb.sqlScan(ctx, v)
}

func (fcgb *FpsClientGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range fcgb.fields {
		if !fpsclient.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := fcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (fcgb *FpsClientGroupBy) sqlQuery() *sql.Selector {
	selector := fcgb.sql.Select()
	aggregation := make([]string, 0, len(fcgb.fns))
	for _, fn := range fcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(fcgb.fields)+len(fcgb.fns))
		for _, f := range fcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(fcgb.fields...)...)
}

// FpsClientSelect is the builder for selecting fields of FpsClient entities.
type FpsClientSelect struct {
	*FpsClientQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (fcs *FpsClientSelect) Scan(ctx context.Context, v interface{}) error {
	if err := fcs.prepareQuery(ctx); err != nil {
		return err
	}
	fcs.sql = fcs.FpsClientQuery.sqlQuery(ctx)
	return fcs.sqlScan(ctx, v)
}

func (fcs *FpsClientSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := fcs.sql.Query()
	if err := fcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
