// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/predicate"
	"git.teko.vn/loyalty-system/loyalty-file-processing/internal/ent/ent/processingfile"
)

// ProcessingFileQuery is the builder for querying ProcessingFile entities.
type ProcessingFileQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ProcessingFile
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProcessingFileQuery builder.
func (pfq *ProcessingFileQuery) Where(ps ...predicate.ProcessingFile) *ProcessingFileQuery {
	pfq.predicates = append(pfq.predicates, ps...)
	return pfq
}

// Limit adds a limit step to the query.
func (pfq *ProcessingFileQuery) Limit(limit int) *ProcessingFileQuery {
	pfq.limit = &limit
	return pfq
}

// Offset adds an offset step to the query.
func (pfq *ProcessingFileQuery) Offset(offset int) *ProcessingFileQuery {
	pfq.offset = &offset
	return pfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pfq *ProcessingFileQuery) Unique(unique bool) *ProcessingFileQuery {
	pfq.unique = &unique
	return pfq
}

// Order adds an order step to the query.
func (pfq *ProcessingFileQuery) Order(o ...OrderFunc) *ProcessingFileQuery {
	pfq.order = append(pfq.order, o...)
	return pfq
}

// First returns the first ProcessingFile entity from the query.
// Returns a *NotFoundError when no ProcessingFile was found.
func (pfq *ProcessingFileQuery) First(ctx context.Context) (*ProcessingFile, error) {
	nodes, err := pfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{processingfile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pfq *ProcessingFileQuery) FirstX(ctx context.Context) *ProcessingFile {
	node, err := pfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProcessingFile ID from the query.
// Returns a *NotFoundError when no ProcessingFile ID was found.
func (pfq *ProcessingFileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{processingfile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pfq *ProcessingFileQuery) FirstIDX(ctx context.Context) int {
	id, err := pfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProcessingFile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProcessingFile entity is found.
// Returns a *NotFoundError when no ProcessingFile entities are found.
func (pfq *ProcessingFileQuery) Only(ctx context.Context) (*ProcessingFile, error) {
	nodes, err := pfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{processingfile.Label}
	default:
		return nil, &NotSingularError{processingfile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pfq *ProcessingFileQuery) OnlyX(ctx context.Context) *ProcessingFile {
	node, err := pfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProcessingFile ID in the query.
// Returns a *NotSingularError when more than one ProcessingFile ID is found.
// Returns a *NotFoundError when no entities are found.
func (pfq *ProcessingFileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{processingfile.Label}
	default:
		err = &NotSingularError{processingfile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pfq *ProcessingFileQuery) OnlyIDX(ctx context.Context) int {
	id, err := pfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProcessingFiles.
func (pfq *ProcessingFileQuery) All(ctx context.Context) ([]*ProcessingFile, error) {
	if err := pfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pfq *ProcessingFileQuery) AllX(ctx context.Context) []*ProcessingFile {
	nodes, err := pfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProcessingFile IDs.
func (pfq *ProcessingFileQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := pfq.Select(processingfile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pfq *ProcessingFileQuery) IDsX(ctx context.Context) []int {
	ids, err := pfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pfq *ProcessingFileQuery) Count(ctx context.Context) (int, error) {
	if err := pfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pfq *ProcessingFileQuery) CountX(ctx context.Context) int {
	count, err := pfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pfq *ProcessingFileQuery) Exist(ctx context.Context) (bool, error) {
	if err := pfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pfq *ProcessingFileQuery) ExistX(ctx context.Context) bool {
	exist, err := pfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProcessingFileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pfq *ProcessingFileQuery) Clone() *ProcessingFileQuery {
	if pfq == nil {
		return nil
	}
	return &ProcessingFileQuery{
		config:     pfq.config,
		limit:      pfq.limit,
		offset:     pfq.offset,
		order:      append([]OrderFunc{}, pfq.order...),
		predicates: append([]predicate.ProcessingFile{}, pfq.predicates...),
		// clone intermediate query.
		sql:    pfq.sql.Clone(),
		path:   pfq.path,
		unique: pfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID int64 `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProcessingFile.Query().
//		GroupBy(processingfile.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pfq *ProcessingFileQuery) GroupBy(field string, fields ...string) *ProcessingFileGroupBy {
	grbuild := &ProcessingFileGroupBy{config: pfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pfq.sqlQuery(ctx), nil
	}
	grbuild.label = processingfile.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID int64 `json:"client_id,omitempty"`
//	}
//
//	client.ProcessingFile.Query().
//		Select(processingfile.FieldClientID).
//		Scan(ctx, &v)
func (pfq *ProcessingFileQuery) Select(fields ...string) *ProcessingFileSelect {
	pfq.fields = append(pfq.fields, fields...)
	selbuild := &ProcessingFileSelect{ProcessingFileQuery: pfq}
	selbuild.label = processingfile.Label
	selbuild.flds, selbuild.scan = &pfq.fields, selbuild.Scan
	return selbuild
}

func (pfq *ProcessingFileQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pfq.fields {
		if !processingfile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pfq.path != nil {
		prev, err := pfq.path(ctx)
		if err != nil {
			return err
		}
		pfq.sql = prev
	}
	return nil
}

func (pfq *ProcessingFileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProcessingFile, error) {
	var (
		nodes = []*ProcessingFile{}
		_spec = pfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ProcessingFile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ProcessingFile{config: pfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pfq *ProcessingFileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pfq.querySpec()
	_spec.Node.Columns = pfq.fields
	if len(pfq.fields) > 0 {
		_spec.Unique = pfq.unique != nil && *pfq.unique
	}
	return sqlgraph.CountNodes(ctx, pfq.driver, _spec)
}

func (pfq *ProcessingFileQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pfq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (pfq *ProcessingFileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   processingfile.Table,
			Columns: processingfile.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: processingfile.FieldID,
			},
		},
		From:   pfq.sql,
		Unique: true,
	}
	if unique := pfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, processingfile.FieldID)
		for i := range fields {
			if fields[i] != processingfile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pfq *ProcessingFileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pfq.driver.Dialect())
	t1 := builder.Table(processingfile.Table)
	columns := pfq.fields
	if len(columns) == 0 {
		columns = processingfile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pfq.sql != nil {
		selector = pfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pfq.unique != nil && *pfq.unique {
		selector.Distinct()
	}
	for _, p := range pfq.predicates {
		p(selector)
	}
	for _, p := range pfq.order {
		p(selector)
	}
	if offset := pfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProcessingFileGroupBy is the group-by builder for ProcessingFile entities.
type ProcessingFileGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pfgb *ProcessingFileGroupBy) Aggregate(fns ...AggregateFunc) *ProcessingFileGroupBy {
	pfgb.fns = append(pfgb.fns, fns...)
	return pfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pfgb *ProcessingFileGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pfgb.path(ctx)
	if err != nil {
		return err
	}
	pfgb.sql = query
	return pfgb.sqlScan(ctx, v)
}

func (pfgb *ProcessingFileGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pfgb.fields {
		if !processingfile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pfgb *ProcessingFileGroupBy) sqlQuery() *sql.Selector {
	selector := pfgb.sql.Select()
	aggregation := make([]string, 0, len(pfgb.fns))
	for _, fn := range pfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pfgb.fields)+len(pfgb.fns))
		for _, f := range pfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pfgb.fields...)...)
}

// ProcessingFileSelect is the builder for selecting fields of ProcessingFile entities.
type ProcessingFileSelect struct {
	*ProcessingFileQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pfs *ProcessingFileSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pfs.prepareQuery(ctx); err != nil {
		return err
	}
	pfs.sql = pfs.ProcessingFileQuery.sqlQuery(ctx)
	return pfs.sqlScan(ctx, v)
}

func (pfs *ProcessingFileSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pfs.sql.Query()
	if err := pfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
